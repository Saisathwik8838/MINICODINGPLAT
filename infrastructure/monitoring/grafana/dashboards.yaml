apiVersion: 1

providers:
  - name: 'MiniLeetCode Dashboards'
    folder: 'Platform Metrics'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
